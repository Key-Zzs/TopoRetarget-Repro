# Stage-12 ContactPose mug closeout

The static one-frame ContactPose mug selection uses official annotated joints
and `MANO_CONTRACT_V2`; fitted MANO remains visual-only. It is a static contact
evaluation sample, so temporal metrics are `NOT_APPLICABLE`. The source object
is the original mug mesh and the final audit remains independent from the
active QuerySet.

The status-8 closeout repaired generic stale slack feasibility bookkeeping and
retained the rejected diagnostic. Two independent strict replays are required
to agree before a formal artifact is promoted. The Stage-12 ContactPose status
does not claim that Eq.10/Eq.11 ContactPose contact benchmarking was reproduced:
`contact_annotation_available` remains the adapter's recorded value and the
paper contact benchmark remains `NOT_REPRODUCED`.

The final qualification and all generated HTML/report evidence are kept under
ignored `.local` paths; no dataset payload or formal historical artifact is
overwritten.
